run_session_params:
  file_path: "run_session.log"
  app_name: "agents"
  user_id: "default"

retry_config_params:
  file_path: "retry_config.log"
  attempts: 5
  exp_base: 7
  initial_delay: 1
  http_status_codes: [429, 500, 503, 504]

pinecone_memory_params:
  file_path: "pinecone_memory.log"
  embedding_model: "all-MiniLM-L6-v2"

postgre_memory_db:
  file_path: "postgre_database.log"

user_profiler_agent:
  file_path: "user_profiling.log"
  model_name: "gemini-2.5-flash"

meal_planner_agent:
  file_path: "meal_planner_agent.log"
  model_name: "gemini-2.5-flash"

prompt_loader:
  file_path: "prompt_loader.log"